package io.github.aldefy.sidesheet

/**
 * The edge of the screen from which a side sheet appears.
 */
enum class SideSheetEdge {
    /** The sheet slides in from the start (left in LTR, right in RTL). */
    Start,

    /** The sheet slides in from the end (right in LTR, left in RTL). */
    End,
}
